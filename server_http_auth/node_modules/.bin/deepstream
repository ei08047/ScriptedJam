#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../deepstream.io/bin/deepstream" "$@"
  ret=$?
else 
  node  "$basedir/../deepstream.io/bin/deepstream" "$@"
  ret=$?
fi
exit $ret
